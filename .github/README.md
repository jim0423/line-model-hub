# Workflows

Workflow files have been temporarily removed for initial push — they require a PAT with `workflow` scope.

See `/tmp/.github-workflows-backup/` for the build-windows.yml to restore manually.
