[workspace]
resolver = "2"
members = [
    "crates/line-hub-core",
    "crates/line-hub-mcp",
    "crates/line-hub-tauri",
]

[workspace.package]
version = "0.5.0"
edition = "2021"
rust-version = "1.75"
license = "MIT"
authors = ["Jim <jim@tt-openclaw>"]
repository = "https://github.com/jim/line-model-hub"

[workspace.dependencies]
# Async runtime
tokio = { version = "1.40", features = ["full"] }
futures = "0.3"
async-trait = "0.1"
pin-project-lite = "0.2"

# HTTP / streaming
reqwest = { version = "0.12", default-features = false, features = ["json", "stream", "rustls-tls"] }
reqwest-eventsource = "0.6"
eventsource-stream = "0.2"
bytes = "1.7"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
schemars = "0.8"

# Error / logging
thiserror = "1.0"
anyhow = "1.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# Config
dotenvy = "0.15"

# Crypto for API key encryption
keyring = { version = "3.6", features = ["windows-native", "apple-native", "linux-native-sync-persistent"] }
aes-gcm = "0.10"

# Local persistence
rusqlite = { version = "0.32", features = ["bundled"] }
chrono = { version = "0.4", default-features = false, features = ["clock", "serde"] }

# MCP protocol (JSON-RPC)
jsonrpc-core = "18.0"

[profile.release]
lto = "thin"
codegen-units = 1
opt-level = 3
strip = true

[profile.dev]
opt-level = 0
debug = true